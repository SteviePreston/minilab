### Basic To Do's for the mini lab:

- finish setting up k9s config 
- fix jaeger and otel 
- set up istio and envoy service mesh
- create dashboards for hardware, k3s cluster, applications
- create SLIs with alerting through discord webhook based on prometheus
- create SLO documents.
- create a test application to test everything (probs a portfolio)
- create a reusable ci for GHA
- Setup harbor
- setup a domain name ingress controller and cert-manager.
- set up users and RBAC and SSO.

